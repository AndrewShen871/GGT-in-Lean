import Mathlib
import Cay.QuasiIsometry.Basic
import Cay.QuasiIsometry.CayleyGraphs
import Cay.QuasiIsometry.Equivalence

set_option linter.style.longLine false

/- Add explanation of file here. -George -/

------

-- Below are some to dos. Some may belong in the respective subfiles/already been done in them.

-- Definition of quasi-isometric embedding + quasi-isometry

-- Theorem: Composition of a q.i. embedding is a q.i.

-- Theorem: Composition of a q.i. is a q.i.

-- Definition of quasi-inverse

-- Theorem: A q.i. embedding f:X -> Y is a q.i. iff f has a quasi-inverse.

-- Theorem: If f: X -> Y is a q.i., and g: Y -> X is a quasi-inverse of f, then g is a q.i.

-- Theorem: q.i. is an equivalence relation on the collection of all metric spaces.

-- Examples: (We don't necessarily need all of these, but would be good practice/maybe nice to include.)
-- Floor function is a q.i. from ℤ to ℝ.
-- ℝ^2 with taxicab metric is q.i. to ℝ^2 with Euclidean metric. Same with 2 replace with n ∈ ℕ
