import Mathlib
import Cay.Wordmetric.Wordmetric

set_option linter.style.longLine false

-- We can maybe merge wordmetric and wordlength? -George

------

/- Add explanation of file here. -George -/

------

-- Below are some to dos. Some may belong in the respective subfiles/already be there.

-- Define the word metric of G with generating set S as a min over ways to write an element.

-- Theorem: The word metric defined as above is equivalent to the path metric in QCay(G,S) and SCay(G,S)

-- Theorem: Two finite generating sets determine BiLipschitz word metrics.

-- Theorem: Two finite generating sets determine q.i. word metrics.

-- Theorem: (G,d_S) is q.i. to the geometric realization of the Cayley graph of G w.r.t. S.
